🚨 Your Microservices interview gets serious when the interviewer asks “what would you do?”

1. API latency suddenly increases, but CPU and memory are normal. Where do you investigate?

2. Kafka consumer lag keeps increasing even though consumers are healthy. Why?

3. One downstream service becomes slow and starts affecting your entire application. How do you prevent it?

4. Two requests create the same payment. How would you make the operation idempotent?

5. Database connections get exhausted during peak traffic. What would you check?

6. Your service returns 500 randomly, but there are no obvious errors in the logs. How do you debug it?

7. A retry mechanism starts creating duplicate transactions. What's wrong?

8. A scheduled job executes multiple times after scaling to multiple instances. How do you fix it?

9. Adding more pods doesn't reduce API latency. What could be the bottleneck?

10. A request passes through 7 microservices. How would you trace it end-to-end?

11. Cache improves performance but starts returning stale data. How would you handle it?

12. One service goes down during a critical transaction. How should the system recover?

13. Kafka delivers the same event twice. How would you handle duplicate processing?

14. Production is slow, but staging works perfectly. What differences would you verify?

15. Your service suddenly starts timing out under load. What's the first thing you'd investigate?