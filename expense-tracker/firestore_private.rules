rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /expenses/{docId} {
      // Allow read/write only if the user is authenticated and has your email
      allow read, write:
        if request.auth != null && request.auth.token.email == "your_email@gmail.com";
    }

  }
}
